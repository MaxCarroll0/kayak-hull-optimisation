# Build Instructions
## Linux
Ensure Python3, pip, and Just are installed. Then, run `just setup` in the root dir.

Build the project with `just build`.

## Nix
Or, if using nix, then use the following [flake](https://github.com/MaxCarroll0/kayak-hull-optimisation-flake).

Build the project with `just build`.
