"""
Kernel for the stability (righting moment) curve
"""

class Kernel:
  def __init__(self, kayaker_weight, hull, fluid_velocity, hull_velocity):
    # Initialiser based on constant values
    return None
  
  def k(self, theta1, phi1, theta2, phi2):
    """
      Relate 2d points (theta, phi) where theta is hull (fluid) angle and phi is hull tilt (heel angle) 
    """
    # Actual kernel
    return 0.0
